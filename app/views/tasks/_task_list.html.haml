- if tasks.empty?
  %span.text-warning There is no task!
- else
  %table.table.table-hover.table-bordered
    %thead
      %tr
        %th Title
        %th Created at
        %th Actions
    %tbody
      - tasks.each do |task|
        %tr
          %td
            = link_to task_path(task), remote: true, method: :get do
              #{task.title}
          %td.text-info= task.created_at
          %td
            = link_to task_path(task), remote: true, method: :delete, data: {confirm: "Are you sure you want to delete task #{task.title}?"} do
              %i.icon-remove
            = link_to edit_task_path(task), remote: true do
              %i.icon-edit